# Copia este archivo como .env y rellena con tus valores reales.
# El .env NO se sube a GitHub (está en el .gitignore).

# URL del webhook de n8n (producción)
# Sustituye TU_SERVIDOR por la IP o dominio de tu servidor n8n.
N8N_WEBHOOK_URL=http://TU_SERVIDOR:5678/webhook/recruitai
